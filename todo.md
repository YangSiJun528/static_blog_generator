## 정적 블로그 생성기 요구사항

### 기술 스택

- 사용 언어는 TS
- 기능을 적절히 분리하여 테스트 기능을 제공해야 함.
- `markdown-it-obsidian.js` 파일을 참고해 옵시디언 형식의 링크를 마크다운 형식의 링크로 변경하는 기능 또한 제공되어야 함.
- 파서, 옵시디언 변환기, 정적 파일 생성기, 토글 생성기 등은 적절하게 분리되어 제공되어야 함.
- 폴더 구조는 다음과 같음
  - notes: .md 파일들이 저장되는 폴더. 하위 여러 폴더를 가질 수 있음.
  - files: notes 내부의 .md 파일들이 바라보는 파일들. 하위 여러 폴더를 가질 수 있음.
  - assets: html 페이지 구현에 필요한 파일들, 주로 파비콘 이미지 등이 포함됨. 하위 여러 폴더를 가질 수 있음.
  - output: notes 파일을 기반으로 만들어지는 html 파일 및 결과물이 여기 포함됨. 정적 페이지를 배포한다면 이 폴더를 루트로 배포할 수 있어야 함.
    - 따라서 assets나 files도 복사되어 제공되어야 함.

### 🗂 1. **사이트 구조**

* 전체 블로그는 디렉토리 트리 구조를 그대로 반영한 **정적 파일 기반 사이트**로 구성됨.
  * nginx autoindex의 스타일을 가짐.
* 하위 페이지에 접근 가능한 2가지 방법이 있다.
  * 링크: 현재 페이지에서 폴더나 파일 이름에 링크가 있다. 각 링크는 클릭 시 해당 폴더나 파일의 HTML 페이지로 이동함 (페이지 전환 있음). 
  * 토글: 하위 폴더나 파일 이름의 좌측에 리스트 아이콘이 토글로 변경되어있으며, 토글을 사용해서 내용을 열고 닫을 수 있다. 
    * 토글을 사용한 콘텐츠 로드 시 내용을 그대로 동적으로 로드한다. 페이지 이동 없이 수행한다.

---

### 📄 2. **페이지 설명**

1. 공유 템플릿
  - 모든 페이지가 공유햐는 형태
  1. 헤더
     - 상단에 현재 경로 (예: `/posts/2025/07`)가 헤더에 표시됨. (nginx autoindex의 스타일)
      - 파일의 경우 현재 파일 이름까지 제공.
      - 경로 폴더 별로 페이지 이동이 가능함.
  2. 푸터
     - 간단한 문구가 들어감. 생성기 내부에서 하드코딩되어 제공.

2. **디렉토리 페이지 (폴더)**
* 내부에 있는 파일/디렉토리 리스트를 토글 가능한 아이콘과 함께 보여줌. (토글이 열린 상태임.)

3. **마크다운 파일 페이지 (파일)**
* 최상단에 해당 파일이 위치한 트리 구조를 토글 UI로 닫힌 상태로 표시함.
  예: `/posts/2025/07/my-post.md` 위치라면,  `/posts/2025/07` 해당 트리를 상단에 압축된 상태로 제공.
* 본문은 마크다운을 HTML로 변환해서 본문 형태로 제공됨.
* 스타일은 최소한이며, CSS 없이도 읽을 수 있는 구조로 설계됨.

---
### 🔄 3. **토글 기능**

* 각 폴더나 파일 항목은 **옆에 토글 아이콘**이 있음.
* 클릭하면 서버 요청 없이 **정적 파일로부터 콘텐츠 전부 로드**

---

### ✅ 4. **제한 및 조건**

* 마크다운 파일만 콘텐츠로 포함됨. `.md` 확장자만 처리 대상. (다른 파일이 있으면 에러 발생)
* 그 외 파일 (이미지, PDF 등)은 링크로 제공되고 실제 내용은 포함하지 않음.
* 루트가 아닌 위치에서도 항상 동일한 구조와 스타일 유지됨 (상대 경로 관리 필수).
* CSS는 거의 사용하지 않음. 시각적 구조는 HTML 기본 요소로 충분히 전달됨.

---

### 📦 6. **생성기 동작 개요**

* 특정 디렉토리를 입력으로 받아 HTML 파일들을 생성.
* 각 디렉토리에 대해:
  * `index.html` 생성
  * 마크다운 파일은 대응되는 `.html` 파일 생성
  * 상대 링크로 연결되어 어느 위치에서도 동일하게 동작함.

''
